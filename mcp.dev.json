{
  "_comment": "Local dev only: copy to mcp.json while testing upstream MCP builds. Replace REPO_ROOT with your memwal-agent-memory clone path. Restore production mcp.json (npx @memwalpp/mcp) before Marketplace releases.",
  "mcpServers": {
    "memwal-agent-memory": {
      "command": "node",
      "args": [
        "REPO_ROOT/packages/mcp/dist/bundle.mjs",
        "--transport",
        "stdio"
      ],
      "env": {
        "MEMWAL_NAMESPACE": "cursor",
        "MEMWAL_MCP_DATA_DIR": "${userHome}/.memwal-agent-memory/mcp"
      }
    }
  }
}
