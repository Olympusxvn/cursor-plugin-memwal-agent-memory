{
  "_comment": "Use ONLY for local testing before @memwalpp/mcp is published to npm. Replace REPO_ROOT with your memwal-agent-memory clone path. Do NOT submit this file to the marketplace.",
  "mcpServers": {
    "memwal-agent-memory": {
      "command": "node",
      "args": ["REPO_ROOT/packages/mcp/dist/cli.js", "--transport", "stdio"],
      "env": {
        "MEMWAL_NAMESPACE": "cursor",
        "MEMWAL_MCP_DATA_DIR": "${userHome}/.memwal-agent-memory/mcp"
      }
    }
  }
}
